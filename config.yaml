thresholds:
  port_scan:
    unique_ports: 15
    time_window: 10

  ssh_bruteforce:
    attempts: 5
    time_window: 60

  syn_flood:
    syn_threshold: 100
    time_window: 5

  dns_anomaly:
    request_threshold: 50
    time_window: 10

logging:
  level: INFO
  file: sentinel.log
